node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local-only case/document archive — never publish
docs/

# Agent / session-tooling folders (Claude Code, remember tool)
.remember/
.claude/
CLAUDE.md

# One-time integration handoff — local only, never ships
handoff/
